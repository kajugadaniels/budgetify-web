// TODO (Step 5): Build the full email OTP + Google login page.
export default function LoginPage() {
  return (
    <div className="text-text-secondary text-sm">Login — coming in Step 5</div>
  );
}
